1. What is differences between <head> and  <header> tags?
Answer: Head tag <head> metadata, links, title </head> : The head tag is used for document title, styling, scripts, adding metadeta, links, etc. 
        Header tag <header> h1, p(some content) </header> : The header tag is used for headers as seen in articles. . 

Reference: https://www.youtube.com/watch?v=sptTVoBi4sk

2. How to allow only numbers in textbox?
Answer: <input type="number">
