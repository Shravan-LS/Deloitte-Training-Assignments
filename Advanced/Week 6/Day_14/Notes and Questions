What are pipes in Angular?? Pre-defined Pipes
Answer: Angular Pipes transform the output. They are mainly used to format the data before displaying it to the user. 
        A pipe takes expression value as an input and transforms it into the desired output. Below is the list of inbuilt pipes in Angular:
        Uppercase
        Lowercase
        titlecase
        currency
        date
        slice
        percent
        decimal
        Icon
        i18nselect
        i18nplural

2. What is Gitbash?
Answer: Git Bash is an application for Microsoft Windows environments which provides an emulation layer for a Git command line experience. 
        Bash is an acronym for Bourne Again Shell. A shell is a terminal application used to interface with an operating system through written commands. 
        Bash is a popular default shell on Linux and macOS. Git Bash is a package that installs Bash, some common bash utilities, 
        and Git on a Windows operating system.

4.   How to use DecimalPipe and PercentPipe in Angular?  Check with an example?
Answer:  Percent Pipe: Transforms a number to a percentage string, formatted according to locale rules.
         Decimal Pipe: It is used to format a number as decimal number according to locale rules.
         Example: {{ rounding_value | number : '1.2-2'}} will round a number to 2 decimal points.

         (For more reading on the same, refer: https://www.angularjswiki.com/angular/angular-decimal-pipe/)